# domain.md — 도메인 로직·규칙 (도메인 수정 시 읽기)

- 기능·규칙은 가능하면 코드가 아니라 데이터로 추가한다 (함수를 건드리지 않고 안전하게 확장).
- 정책은 `docs/policies/POL.md` 를 단일 출처로 삼는다. 여기에 값을 중복 기재하지 않는다.

## 이 프로젝트 고유 도메인 규칙
- (TODO 예: 모든 영속 동작은 `XxxStore` 의 메서드를 통해서만 한다. 뷰가 저장소에 직접 접근 금지)
- (TODO 예: 정렬은 한 곳에서만 — POL-### 단일 출처)
- (TODO 예: 입력 검증은 `Xxx.normalize()` 단일 함수로. 입력 경로마다 다른 검증 금지)
- (TODO 예: 시간은 외부 주입 Clock으로만. `Date()` 직접 호출 금지)
- (TODO)
