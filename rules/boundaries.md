# boundaries.md — 핵심 제약·금지선 (항상 적용)  [가드레일: 금지 + 대안 + 왜]

> '하지 말 것' + 대안 + 이유. 이유 없는 금지는 시간이 지나면 아무도 못 건드리는 족쇄가 된다.
> 도메인·스택별 가드레일을 추가한다. 예시는 비워두지 말고 실제 값으로 채운다.

| 하지 말 것 | 대안 | 왜 |
| --- | --- | --- |
| (TODO 예: 시간 직접 호출 `Date()/now`) | (TODO 예: Clock 주입) | (TODO 예: '30일 후' 시나리오를 테스트로 결정적 검증) |
| (TODO 외부 라이브러리 임의 추가) | (TODO 표준 API 우선 / 추가 시 사람 승인) | (TODO 단순함·자립성·하네스 유지) |
| (TODO 카피·콘텐츠를 AI가 지어냄) | (TODO 사람이 작성) | (TODO 콘텐츠의 결은 사람이 정한다) |
| (TODO) | (TODO) | (TODO) |

## 번호 가이드 (이렇게 할 것 — "#3 위반"처럼 짧게 지적용)
- #1  (TODO 대상 플랫폼/런타임을 명시 선언)
- #2  (TODO Core(로직)과 App(UI)를 엄격히 분리)
- #3  (TODO 외부 의존성 0 — 불가피하면 사유 + 사람 승인)
- #4  (TODO 새로 채택한 API/패턴은 주석에 명시)
- #5  반드시 지킬 규칙은 기계 강제(테스트·린터·훅·CI)와 짝짓는다
